import time

# Define a timestamp
timestamp = time.time()

# # Define the precision (number of decimal places) to round off to
# precision = 0

# Round off the timestamp to the specified precision
rounded_timestamp = round(timestamp)

print(f"Original timestamp: {timestamp}")
print(f"Rounded timestamp: {rounded_timestamp}")
